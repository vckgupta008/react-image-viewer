import { makeStyles } from '@material-ui/core/styles';
const useStyles = makeStyles({
    header: {
        background: '#263238',
        color: 'white',
        fontSize: '18px',
        padding: '20px 15px',
    },
});

export default useStyles;